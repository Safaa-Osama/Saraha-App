A backend application that simulates the core functionality of the famous anonymous messaging platform Saraha.
The app allows users to create accounts and receive anonymous messages from others without revealing the sender's identity.

// "dev": "cross-env NODE_ENV=development pm2 start src/index.js -i 0",
